# ZeroQuant - Claude 세션 컨텍스트

> 마지막 업데이트: 2026-02-03 | 버전: v0.6.4

---

## 🚀 작업 시작 전 확인사항

| 문서 | 위치 | 용도 |
|------|------|------|
| **TODO** | `docs/todo.md` | 현재 진행 중/남은 작업 |
| **PRD** | `docs/prd.md` | 제품 요구사항 문서 |
| **개발 규칙** | `docs/development_rules.md` | 신규 기능 추가 시 필수 규칙 |
| **개선사항** | `docs/improvement_todo.md` | 코드베이스 개선 로드맵 |

---

## 📊 프로젝트 개요

**ZeroQuant**: Rust 기반 고성능 다중 시장 자동화 트레이딩 시스템

| 항목 | 수치 |
|------|------|
| Rust 파일 | 180+ |
| Crate 수 | 10개 |
| 전략 수 | 26개 |
| API 라우트 | 24개 |
| 마이그레이션 | 23개 |

### 기술 스택
- **Backend**: Rust (Tokio, Axum, SQLx)
- **Database**: PostgreSQL (TimescaleDB), Redis
- **Frontend**: SolidJS, TypeScript, Vite
- **ML**: ONNX Runtime
- **Infrastructure**: Podman

---

## 🤖 서브에이전트 활용 가이드 (필수)

> ⚠️ **복잡한 작업은 반드시 서브에이전트를 활용하세요.**

### Task Tool 에이전트 종류

| 에이전트 | 용도 | 예시 |
|----------|------|------|
| **Explore** | 코드베이스 탐색, 구조 파악 | "에러 핸들링은 어디서?", "API 라우트 구조" |
| **Plan** | 구현 계획 수립, 아키텍처 설계 | "인증 시스템 구현 계획", "리팩토링 전략" |
| **Bash** | Git 작업, 빌드, 테스트 | "cargo test 실행", "git 커밋" |
| **general-purpose** | 복잡한 멀티스텝 작업 | "버그 원인 분석 후 수정안 제시" |

### 사용 원칙

1. **탐색 작업** → `Explore` 에이전트 사용
   - ❌ 직접 Glob/Grep 반복 호출
   - ✅ Task tool로 Explore 에이전트에 위임

2. **복잡한 구현** → `Plan` 에이전트로 계획 수립
   - ❌ 바로 코드 작성 시작
   - ✅ 먼저 계획 수립 후 사용자 승인

3. **독립 작업** → 병렬 에이전트 실행
   - ❌ 순차적으로 하나씩 처리
   - ✅ 의존성 없는 작업은 동시에 Task 호출

4. **API 검증** → Context7 활용
   - ❌ 학습 데이터 기반 추측
   - ✅ `resolve-library-id` → `query-docs` 순서로 검증

5. **코드 탐색** → Serena MCP 우선 사용
   - ❌ 단순 Grep으로 텍스트 검색
   - ✅ Serena의 semantic tools로 심볼 기반 탐색
   - ℹ️ Grep은 문자열 패턴 매칭에만 제한적 사용

### 예시

```
# 코드베이스 탐색
Task(subagent_type="Explore", prompt="인증 미들웨어 구현 위치와 패턴 분석")

# 구현 계획 수립
Task(subagent_type="Plan", prompt="JWT 인증 추가 구현 계획")

# 병렬 탐색 (한 메시지에 여러 Task 호출)
Task(subagent_type="Explore", prompt="API 라우트 구조")
Task(subagent_type="Explore", prompt="에러 타입 정의")

# Serena를 사용한 코드 탐색
mcp__serena__find_symbol(name_path_pattern="MyClass", relative_path="crates/trader-core")
mcp__serena__find_referencing_symbols(name_path="MyClass", relative_path="crates/trader-core/src/domain/mod.rs")
mcp__serena__get_symbols_overview(relative_path="crates/trader-api/src/routes/strategies.rs")
```

---

## 🐳 인프라 환경 (중요)

> ⚠️ **PostgreSQL과 Redis는 Podman 컨테이너에서 실행됩니다.**

```bash
# DB 접속
podman exec -it trader-timescaledb psql -U trader -d trader

# Redis 접속
podman exec -it trader-redis redis-cli
```

| 항목 | 값 |
|------|-----|
| DB 사용자 | `trader` |
| DB 비밀번호 | `trader_secret` |
| DB 이름 | `trader` |

> 상세 명령어: `docs/infrastructure.md` 참조

---

## 📁 프로젝트 구조

```
zeroquant/
├── crates/
│   ├── trader-core/         # 도메인 모델, 공통 유틸리티
│   ├── trader-exchange/     # 거래소 연동 (Binance, KIS)
│   ├── trader-strategy/     # 전략 엔진, 26개 전략
│   ├── trader-risk/         # 리스크 관리
│   ├── trader-execution/    # 주문 실행 엔진
│   ├── trader-data/         # 데이터 수집/저장 (OHLCV)
│   ├── trader-analytics/    # ML 추론, 성과 분석
│   ├── trader-api/          # REST/WebSocket API
│   ├── trader-cli/          # CLI 도구
│   └── trader-notification/ # 알림 (Telegram)
├── frontend/                # SolidJS + TypeScript + Vite
├── migrations/              # DB 마이그레이션 (23개)
└── scripts/                 # ML 훈련, 스크래퍼
```

---

## ⚠️ 핵심 규칙 요약

| 규칙 | 설명 | 상세 |
|------|------|------|
| 레거시 코드 제거 | 개선 시 불필요/레거시 코드 즉시 제거, 기술 부채 금지 | [§1](#) |
| 즉시 수정 원칙 | 문제 발견 시 나중으로 미루지 말고 즉시 수정, 기술 부채 생성 금지 | [§2](#) |
| 방어적 코딩 | 모든 에러 케이스 처리, Result/Option 활용, panic 방지 | [§3](#) |
| 거래소 중립적 코드 | 특정 거래소 의존 금지, 추상화된 인터페이스 사용 | [§4](#) |
| 이후 작업 고려 | 향후 확장성과 다른 작업 고려하여 설계 | [§5](#) |
| Decimal 타입 필수 | 금융 계산에 `rust_decimal::Decimal` 사용 (f64 금지) | [금융](#) |
| unwrap() 금지 | 프로덕션 코드에서 `unwrap()` 사용 금지 | [에러](#) |
| 학습 데이터 의존 금지 | Context7로 최신 API 검증 필수 | [검증](#) |
| 주석은 한글로 | 모든 주석은 한글로 작성 | - |
| 서브에이전트 활용 | 복잡한 탐색/계획은 Task tool 사용 | - |
| 컨테이너 접속 | `podman exec` 사용, 로컬 CLI 금지 | [인프라](#) |

> 💡 **상세 규칙**: `docs/development_rules.md` 참조 (v1.1, 180+ 규칙)
> 💡 **에이전트 가이드**: `docs/agent_guidelines.md` 참조

---

## 🤖 전용 Agent MCP (자동화 도구)

> **위치**: `.agents/zeroquant_mcp/`
> **용도**: 빌드, 리뷰, 설계 등을 **자동으로 실행**

### 설치 (최초 1회)

```bash
# 1. 의존성 설치
cd .agents
pip install -r zeroquant_mcp/requirements.txt

# 2. Claude Code 설정에 MCP 서버 추가
# ~/.claude/config.json
```

상세: `.agents/INSTALL.md` 참조

### 사용 가능한 Agent

> ✅ **검증 완료** (2026-02-03): 모든 에이전트 동작 확인 완료 (`test_agents.py`)

| Agent | 기능 | 실행 시간 | 검증 |
|-------|------|--------:|:----:|
| **build_validator** | cargo build/clippy/test/fmt 자동 실행 | ~30초 | ✅ |
| **security_reviewer** | 보안 취약점 자동 검출 (금융 특화) | ~10초 | ✅ |
| **code_reviewer** | 코드 품질 자동 리뷰 (6개 항목) | ~5초 | ✅ |
| **test_writer** | 테스트 자동 생성 및 커버리지 분석 | ~5초 | ✅ |
| **ux_reviewer** | UX 자동 평가 (점수 산출) | ~15초 | ✅ |
| **code_simplifier** | 중복/복잡도/레거시 자동 분석 | 4-8시간 | ⚠️ |
| **code_architect** | 아키텍처 설계 문서 생성 | 2-4시간 | ⚠️ |
| **release_manager** | 릴리즈 자동화 (문서+커밋+푸시) | ~10초 | ⚠️ |

**주요 사용 예시**:
```python
# 커밋 전 빠른 체크 (권장)
mcp__zeroquant-agents__security_reviewer(target="staged")
mcp__zeroquant-agents__build_validator()
mcp__zeroquant-agents__code_reviewer(target="staged")
```

**진행 상황 모니터링**:
- 로그 파일: `~/.claude/logs/mcp-zeroquant-agents.log`
- 모니터링: `tail -f ~/.claude/logs/mcp-zeroquant-agents.log`
- 상세 가이드: `.agents/MONITORING.md` 참조 📊

### 특징

- ✅ **즉시 실행**: 프롬프트 작성 불필요
- ✅ **일관된 결과**: 표준화된 체크리스트
- ✅ **자동화 가능**: CI/CD 통합 가능
- ✅ **빠른 피드백**: 2-5분 내 완료
- 🔒 **보안 강화**: 금융 시스템 특화 보안 체크
- 🧪 **테스트 자동화**: 함수 분석 후 테스트 스켈레톤 생성

---

## 📚 참조 문서

| 문서 | 위치 | 설명 |
|------|------|------|
| **MCP 설치** | `.agents/INSTALL.md` | MCP 서버 설치 가이드 |
| **MCP 사용법** | `.agents/README.md` | 에이전트 상세 사용법, 워크플로우 |
| **MCP 모니터링** | `.agents/MONITORING.md` | 실시간 진행 상황 로그 📊 |
| 인프라 가이드 | `docs/infrastructure.md` | Podman 컨테이너 명령어 |
| 에이전트 가이드라인 | `docs/agent_guidelines.md` | API 검증, Context7 사용법 |
| 시스템 사용 가이드 | `docs/system_usage.md` | 모니터링, CSV 동기화 |
| 아키텍처 | `docs/architecture.md` | 시스템 아키텍처 |
| API 문서 | `docs/api.md` | REST API 명세 |
