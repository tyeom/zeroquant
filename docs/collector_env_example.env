# Standalone Collector 환경변수 예제
# 사용법: cp docs/collector_env_example.env crates/trader-collector/.env

# ============================================
# Database 설정
# ============================================
DATABASE_URL=postgresql://trader:trader_secret@localhost:5432/trader

# ============================================
# 심볼 동기화 설정
# ============================================
# 최소 심볼 수 (이 수 이하일 때만 동기화 실행)
SYMBOL_SYNC_MIN_COUNT=100

# KRX 동기화 활성화 (KOSPI/KOSDAQ)
SYMBOL_SYNC_KRX=true

# Binance 동기화 활성화 (암호화폐)
SYMBOL_SYNC_BINANCE=false

# Yahoo Finance 동기화 활성화 (미국/글로벌)
SYMBOL_SYNC_YAHOO=true

# Yahoo 최대 수집 종목 수
SYMBOL_SYNC_YAHOO_MAX=500

# ============================================
# OHLCV 수집 설정
# ============================================
# 배치당 심볼 수
OHLCV_BATCH_SIZE=50

# 갱신 기준 일수 (마지막 수집 후 N일 경과 시 재수집)
OHLCV_STALE_DAYS=1

# API 요청 간 딜레이 (밀리초)
OHLCV_REQUEST_DELAY_MS=500

# 수집 시작 날짜 (선택, YYYYMMDD 형식)
# OHLCV_START_DATE=20240101

# 수집 종료 날짜 (선택, YYYYMMDD 형식)
# OHLCV_END_DATE=20261231

# ============================================
# Fundamental 수집 설정
# ============================================
# 배치당 심볼 수
FUNDAMENTAL_BATCH_SIZE=50

# 갱신 기준 일수 (기본: 7일)
FUNDAMENTAL_STALE_DAYS=7

# API 요청 간 딜레이 (밀리초, Yahoo Finance 차단 방지)
FUNDAMENTAL_REQUEST_DELAY_MS=2000

# OHLCV 데이터 함께 수집 여부
FUNDAMENTAL_INCLUDE_OHLCV=true

# ============================================
# 데몬 모드 설정
# ============================================
# 워크플로우 실행 주기 (분 단위)
DAEMON_INTERVAL_MINUTES=60

# ============================================
# 로깅 설정
# ============================================
# 로그 레벨: trace, debug, info, warn, error
RUST_LOG=info

# JSON 형식 로그 (Elasticsearch 연동 시)
# RUST_LOG_FORMAT=json
